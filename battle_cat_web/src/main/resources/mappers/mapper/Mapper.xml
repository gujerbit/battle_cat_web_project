<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gujerbit.battle_cat_web.dao.UpdateVersionDAO">
	<select id="selectUpdateVersionTitle" resultType="UpdateVersionVO">
		SELECT DISTINCT title, update_date, version FROM update_version ORDER BY id DESC
	</select>
	<select id="selectUpdateVersion" resultType="UpdateVersionVO">
		SELECT * FROM update_version WHERE version = #{version} ORDER BY id DESC
	</select>
</mapper>